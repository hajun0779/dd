// ---------------------------------------------------------------------------
//  Pterodactyl(프테로닥틸) 호스팅 전용 시작 파일입니다.
//
//  일부 Node.js 에그의 시작 명령에 다음과 같은 버그가 있습니다.
//
//      if [[ "${MAIN_FILE}" == "*.js" ]]; then node ... ; else ts-node --esm ... ; fi
//
//  오른쪽 "*.js" 에 따옴표가 붙어 있어서 패턴 비교가 아니라 글자 그대로의
//  비교가 됩니다. 그래서 MAIN_FILE 을 index.js 로 두면 조건이 항상 거짓이 되고,
//  봇이 node 가 아닌 ts-node 로 실행되어 시작하자마자 죽습니다.
//
//  해결 방법: 패널의 Startup 탭에서 MAIN_FILE 값을 정확히  *.js  로 바꿉니다.
//  그러면 조건이 참이 되어 node 가 이 파일을 실행하고, 이 파일이 index.js 를
//  불러옵니다.
//
//  일반 PC 에서 직접 돌릴 때는 이 파일이 필요 없습니다. node index.js 를 쓰세요.
// ---------------------------------------------------------------------------

import './index.js';
